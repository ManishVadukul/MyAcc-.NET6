@model IEnumerable<MyAcc.Models.Product>


@await Html.Grid(Model).Named("productGrid").Columns(columns =>
{
columns.Add(c => c.Code).Sortable(true).Titled("Code").SetWidth(30);
columns.Add(c => c.Title).Sortable(true).Titled("Item Name");
columns.Add(c => c.Weight).Sortable(true).Titled("Weight");
columns.Add(c => c.UnitName).Sortable(true).Titled("Unit");
columns.Add(c => c.Price).Sortable(true);
columns.Add(c => c.Cost).Sortable(true);
columns.Add(c => c.Perce).Sortable(true).Titled("VAT");
columns.Add(c => c.Name).Sortable(true).Titled("Category");

columns.Add()
        .Encoded(false)
        .Sanitized(false)
        .SetWidth(40)
        .RenderValueAs(o => @<a onclick="showInPopup('@Url.Action("Upsert", "Product", new { id = o.ProductId }, Context.Request.Scheme)','Edit Item')" class="btn btn-warning">Edit</a>);@*)"><i class="fas fa-pencil-alt" style="color:orange;"></i></a>);*@

@*columns.Add().Titled("Delete")
        .Encoded(false)
        .Sanitized(false)
        .SetWidth(30)
        .RenderValueAs(o => @<form asp-action="Delete" asp-route-id="@o.ProductId" onsubmit="return jQueryAjaxDelete(this);" class="d-inline"><button type="submit" style="border: none; background: white"><i class="far fa-trash-alt" style="color:red;"></i></button></form>);*@

}).WithPaging(100).Selectable(true).Sortable().SetTableLayout(GridShared.TableLayout.Fixed, "100%", "650px").RenderAsync()

@*<table class="table">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.Code)</th>
                <th>@Html.DisplayNameFor(model => model.Title)</th>
                <th>@Html.DisplayNameFor(model => model.Price)</th>
                <th>@Html.DisplayNameFor(model => model.Cost)</th>
                <th>@Html.DisplayNameFor(model => model.TaxType.Name)</th>
                <th>@Html.DisplayNameFor(model => model.Category.Name)</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Code)</td>
                    <td>@Html.DisplayFor(modelItem => item.Title)</td>
                    <td>@Html.DisplayFor(modelItem => item.Price)</td>
                    <td>@Html.DisplayFor(modelItem => item.Cost)</td>
                    <td>@Html.DisplayFor(modelItem => item.TaxType.Name)</td>
                    <td>@Html.DisplayFor(modelItem => item.Category.Name)</td>
                    <td>
                        <a onclick="showInPopup('@Url.Action("Upsert","Product",new { id = item.ProductId },Context.Request.Scheme)','New Product')" class="btn btn-warning"><i class="fas fa-pencil-alt"></i> &nbsp; Edit</a>

                        <form asp-action="Delete" asp-route-id="@item.ProductId" onsubmit="return jQueryAjaxDelete(this);" class="d-inline">
                            <input type="submit" value="Delete" class="btn btn-danger" />
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>*@